q-dialog(v-model="dialogActivity" maximized persistent)
    q-card.activitycard
        .flex.column(style="height:100%")
            div.col-grow: iframe(v-if="dialogActivity" src="activity/index.html" ref="activity" @load="activityFN")
            
            div.ending(v-if="idparticipacion").text-center.q-py-md
                div: span.q-pa-sm.bg-white.rounded-borders.text-body1 ID de participación:
                    q-badge(:label="idparticipacion" color="primary")
                .q-ma-md: q-btn(label="Terminar" push color="dark" no-caps type="a" href="https://homedepot.com.mx/")